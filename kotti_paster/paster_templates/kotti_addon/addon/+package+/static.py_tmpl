from fanstatic import (
    Library,
    Resource,
    Group,
    )

library = Library("${project}", "static")
${project}_css = Resource(library, "style.css")
${project}_group = Group([${project}_css])
